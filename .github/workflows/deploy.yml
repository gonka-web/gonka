name: Build-and-deploy

on:
  push:
    branches: ["___main"]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Sync project
        env:
          RSYNC_PASSWORD: BlAm-uD0uebL
        run: rsync -rult --delete --exclude .env --exclude robots.txt ./ rsync_gonka@89.23.101.102::gonka_web
